{
  "servers": {
    "nightfall-photo-ingress": {
      "type": "stdio",
      "command": "${workspaceFolder}/.venv/bin/python",
      "args": [
        "${workspaceFolder}/mcp_server.py",
        "--transport",
        "stdio",
        "--workspace",
        "${workspaceFolder}",
        "--model",
        ".mcp/model.json"
      ]
    }
  }
}
